# 📊 NexusFinance - Система Управления Данными

## ✅ Весь хардкод удален!

Все статические данные заменены на полноценную систему управления с автоматическим сохранением.

---

## 🗂️ Новая Архитектура Данных

### Модели (`Models/`)

1. **`Project.cs`** - Проекты
   - Name, Revenue, Cost, Description
   - Автоматический расчет: Profit, ProfitMargin

2. **`Transaction.cs`** - Транзакции
   - Description, Amount, Date, Category, Project, IsIncome

3. **`Account.cs`** - Счета
   - Name, Institution, Balance, Type

4. **`Investment.cs`** - Инвестиции
   - Name, Type, Amount, CurrentValue
   - Автоматический расчет: Return, ReturnPercent

5. **`Category.cs`** - Категории
   - Name, Type (Income/Expense), Icon

---

## 💾 DataService - Единая Точка Управления

**Расположение:** `Services/DataService.cs`

**Файл данных:** `%APPDATA%\NexusFinance\data.json`

### Методы:

#### Projects
- `GetProjects()` - получить все проекты
- `AddProject(project)` - добавить проект
- `UpdateProject(oldName, newProject)` - обновить проект
- `DeleteProject(name)` - удалить проект

#### Transactions
- `GetTransactions()` - получить все транзакции
- `AddTransaction(transaction)` - добавить транзакцию
- `DeleteTransaction(transaction)` - удалить транзакцию

#### Accounts
- `GetAccounts()` - получить все счета
- `AddAccount(account)` - добавить счет
- `UpdateAccount(oldName, newAccount)` - обновить счет
- `DeleteAccount(name)` - удалить счет

#### Investments
- `GetInvestments()` - получить все инвестиции
- `AddInvestment(investment)` - добавить инвестицию
- `UpdateInvestment(oldName, newInvestment)` - обновить инвестицию
- `DeleteInvestment(name)` - удалить инвестицию

#### Categories
- `GetCategories()` - получить все категории
- `AddCategory(category)` - добавить категорию
- `DeleteCategory(name)` - удалить категорию

**Все изменения автоматически сохраняются в JSON!**

---

## 🎯 Обновленные ViewModels

### 1. **DashboardViewModel** ✅ Без хардкода
**Было:**
```csharp
private decimal _netWorth = 2_450_000; // Хардкод
RecentTransactions = new() {
    new("04.02.2026", "Salary", ...), // Хардкод
};
```

**Стало:**
```csharp
private readonly DataService _dataService;

private void LoadDashboardData()
{
    var transactions = _dataService.GetTransactions();
    var accounts = _dataService.GetAccounts();
    var investments = _dataService.GetInvestments();
    
    NetWorth = accounts.Sum(a => a.Balance) + 
               investments.Sum(i => i.CurrentValue);
    
    // Динамический расчет из реальных данных
}
```

**Функциональность:**
- ✅ Net Worth = сумма счетов + инвестиций (real-time)
- ✅ Monthly Income/Expense = за последние 30 дней
- ✅ Recent Transactions = последние 10 транзакций
- ✅ Top Expenses = топ-5 категорий расходов
- ✅ Команда `RefreshCommand` для обновления

---

### 2. **ProjectAnalyticsViewModel** ✅ Полное управление

**Новые команды:**
- `AddProjectCommand` - открывает диалог добавления
- `EditProjectCommand` - открывает диалог редактирования
- `DeleteProjectCommand` - удаляет с подтверждением
- `SelectProjectCommand` - выбор проекта для анализа

**UI обновления:**
- Кнопка "➕ Add Project" в заголовке
- Кнопки ✏️ Edit и 🗑️ Delete на каждом проекте
- Автообновление статистики при изменениях

---

### 3. **WalletViewModel** ✅ Без хардкода

**Было:**
```csharp
Accounts = new() {
    new("Checking", 450000, ...), // Хардкод
};
```

**Стало:**
```csharp
private void LoadWalletData()
{
    var accountsList = _dataService.GetAccounts();
    var investmentsList = _dataService.GetInvestments();
    
    Accounts = new ObservableCollection<WalletAccount>(
        accountsList.Select(a => new WalletAccount(...))
    );
    
    TotalBalance = accountsList.Sum(a => a.Balance);
    TotalReturn = investmentsList.Sum(i => i.CurrentValue) - 
                  investmentsList.Sum(i => i.Amount);
}
```

**Новые команды:**
- `RefreshCommand` - обновить данные
- `AddAccountCommand` - добавить счет (placeholder)
- `DeleteAccountCommand` - удалить счет
- `AddInvestmentCommand` - добавить инвестицию (placeholder)
- `DeleteInvestmentCommand` - удалить инвестицию

---

### 4. **TransactionInputViewModel** ✅ Реальное сохранение

**Было:**
```csharp
MessageBox.Show("Transaction Added!"); // Только показ
```

**Стало:**
```csharp
var transaction = new Transaction {
    Description = Description,
    Amount = Amount,
    Date = TransactionDate,
    Category = SelectedCategory,
    Project = SelectedProject ?? "Personal",
    IsIncome = IsIncome
};

_dataService.AddTransaction(transaction); // РЕАЛЬНОЕ СОХРАНЕНИЕ!
```

**Функциональность:**
- ✅ Загрузка списка проектов из данных
- ✅ Динамические категории (Income/Expense)
- ✅ Валидация полей
- ✅ Автоматическое сохранение в JSON
- ✅ Очистка формы после сохранения

---

## 🔧 Диалоги Управления

### ProjectEditorDialog ✅ Реализован

**Файлы:**
- `Views/ProjectEditorDialog.xaml`
- `Views/ProjectEditorDialog.xaml.cs`

**Поля:**
- Project Name (required)
- Description
- Revenue (₽)
- Cost (₽)

**Кнопки:**
- ✅ Save - валидация + сохранение
- ❌ Cancel - закрытие без изменений

**Используется для:**
- Добавления новых проектов
- Редактирования существующих проектов

---

## 📁 Структура Данных (JSON)

**Расположение:** `%APPDATA%\NexusFinance\data.json`

```json
{
  "Projects": [
    {
      "Name": "NexusAI",
      "Revenue": 250000,
      "Cost": 135000,
      "Description": "AI Platform Project",
      "CreatedAt": "2026-02-04T..."
    }
  ],
  "Transactions": [
    {
      "Description": "Salary - NexusAI",
      "Amount": 125000,
      "Date": "2026-02-04T...",
      "Category": "Income",
      "Project": "NexusAI",
      "IsIncome": true
    }
  ],
  "Accounts": [
    {
      "Name": "Checking Account",
      "Institution": "Sberbank",
      "Balance": 450000,
      "Type": "Checking",
      "CreatedAt": "2026-02-04T..."
    }
  ],
  "Investments": [
    {
      "Name": "Bitcoin",
      "Type": "Crypto",
      "Amount": 500000,
      "CurrentValue": 800000,
      "PurchaseDate": "2026-01-01T..."
    }
  ],
  "Categories": [
    {
      "Name": "Income",
      "Type": "Income",
      "Icon": "💰"
    }
  ]
}
```

---

## ✅ Что Работает Сейчас

### Проекты
- ✅ Добавление (с диалогом)
- ✅ Редактирование (с диалогом)
- ✅ Удаление (с подтверждением)
- ✅ Автоматический расчет прибыли/маржи
- ✅ Сохранение в JSON

### Транзакции
- ✅ Добавление через форму
- ✅ Валидация полей
- ✅ Динамические категории
- ✅ Привязка к проектам
- ✅ Сохранение в JSON

### Dashboard
- ✅ Real-time расчет Net Worth
- ✅ Статистика за последние 30 дней
- ✅ Топ-5 расходов
- ✅ Последние 10 транзакций
- ✅ Обновление по команде

### Wallet
- ✅ Список счетов из данных
- ✅ Список инвестиций из данных
- ✅ Автоматический расчет доходности
- ✅ Удаление счетов/инвестиций
- ✅ Total Balance/Return

---

## 🚧 В Разработке

### Accounts & Investments
- ⏳ Диалоги добавления/редактирования
- ⏳ Полное управление через UI

**Временное решение:**
Редактирование через JSON файл:
`%APPDATA%\NexusFinance\data.json`

---

## 🎯 Преимущества Новой Системы

1. **✅ Нет хардкода** - все данные динамические
2. **💾 Автосохранение** - каждое изменение сохраняется
3. **📊 Real-time** - все расчеты в реальном времени
4. **🔄 Синхронизация** - изменения видны везде
5. **🛡️ Безопасность** - данные в защищенной папке пользователя
6. **📝 JSON** - простой формат для резервного копирования
7. **🚀 Расширяемость** - легко добавлять новые типы данных

---

## 📝 Пример Использования

### Добавить новый проект:
1. Открыть "Projects" tab
2. Нажать "➕ Add Project"
3. Заполнить форму
4. Нажать "✅ Save"
5. Проект появится везде автоматически!

### Добавить транзакцию:
1. Нажать "➕ Add Transaction"
2. Выбрать Income/Expense
3. Заполнить поля
4. Нажать "✅ Add Transaction"
5. Dashboard обновится автоматически!

### Удалить проект:
1. Открыть "Projects" tab
2. Нажать 🗑️ на проекте
3. Подтвердить удаление
4. Готово!

---

## 🎉 Результат

**Полностью функциональное приложение с:**
- ✅ Управлением проектами
- ✅ Управлением транзакциями
- ✅ Управлением счетами (частично)
- ✅ Управлением инвестициями (частично)
- ✅ Автоматическими расчетами
- ✅ Персистентным хранилищем данных

**Весь хардкод удален!** 🎯
