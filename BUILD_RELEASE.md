# 🚀 Быстрая сборка Release

## Шаг 1: Создать иконку (один раз)

```powershell
.\create-temp-icon.ps1
```

Это создаст временную иконку `app.ico` с логотипом "NF". Можете заменить на свою позже.

## Шаг 2: Собрать Release

### Вариант А: Лёгкий (~50MB) - требует .NET 8 на целевом ПК
```batch
.\publish-release.bat
```

### Вариант Б: Полный (~150MB) - работает без установки .NET
```batch
.\publish-self-contained.bat
```

## Шаг 3: Запустить

```
bin\Release\net8.0-windows\win-x64\publish\NexusFinance.exe
```

Двойной клик и готово! ✅

---

## 📦 Что включено

- ✅ Single-file executable (один файл)
- ✅ ReadyToRun оптимизация (быстрый старт)
- ✅ Иконка приложения
- ✅ Метаданные версии
- ✅ Подпись сборки

## 🎨 Замена иконки

1. Создайте `.ico` файл (256x256 рекомендуется)
   - Онлайн: https://convertio.co/png-ico/
   - Photoshop/GIMP: Save as ICO
2. Замените `app.ico` в корне проекта
3. Пересоберите: `.\publish-release.bat`

## 📊 Сравнение вариантов

| Характеристика | Framework-Dependent | Self-Contained |
|----------------|---------------------|----------------|
| Размер | ~50 MB | ~150 MB |
| Скорость | Быстрее | Чуть медленнее |
| Требования | .NET 8 Runtime | Нет зависимостей |
| Обновления | Меньше скачивать | Весь runtime каждый раз |
| **Рекомендуется для** | **Разработка, тестирование** | **Публичный релиз** |

## 🔧 Обновление версии

Отредактируйте `NexusFinance.csproj`:

```xml
<AssemblyVersion>1.1.0.0</AssemblyVersion>
<FileVersion>1.1.0.0</FileVersion>
<Version>1.1.0</Version>
```

## ⚡ Командная строка (для CI/CD)

```bash
# Framework-dependent
dotnet publish -c Release -r win-x64 --self-contained false

# Self-contained
dotnet publish -c Release -r win-x64 --self-contained true
```

## 🐛 Проблемы?

- **"Требуется .NET"** → Используйте `publish-self-contained.bat`
- **Нет иконки** → Проверьте `app.ico` в корне проекта
- **Большой размер** → Framework-dependent версия меньше

---

💡 **Совет:** Для распространения пользователям используйте Self-Contained сборку - работает "из коробки".
